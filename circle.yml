test:
  override:
    - mkdir -p $CIRCLE_TEST_REPORTS/mocha
    - node_modules/.bin/mocha test --reporter mocha-junit-reporter mochaFile=$CIRCLE_TEST_REPORTS/mocha/junix.xml
machine:
  node:
    version: 4.2.3